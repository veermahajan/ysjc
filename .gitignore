# macOS
.DS_Store

# gstack browse daemon state (per-session, not source)
.gstack/

# Node / Astro (site/ has its own .gitignore but repeat at root for safety)
node_modules/
dist/
.astro/
.env
.env.production

# Editor metadata
.idea/
.vscode/
